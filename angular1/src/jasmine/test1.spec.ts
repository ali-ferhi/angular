
describe("suite of specs", () => {

    beforeAll( () => {

    });

    beforeEach( () => {

    });

    it("signle spec", () => {
        
        /* arrage */
        let arg = 4;

        /* act */
        let res = arg * 2;

        /* assert */
        expect(res).toBe(8);       
    });

    afterEach( () => {
        
    });

    afterAll( () => {

    });

    describe("embeded suite of specs", () => {

    });
    
});